import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    title: {
        fontSize: 60,
        alignContent: 'center'
    }
})

export default styles